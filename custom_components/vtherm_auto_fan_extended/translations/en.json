{
    "title": "Versatile Thermostat Auto Fan",
    "config": {
        "step": {
            "user": {
                "title": "Auto Fan — target thermostat",
                "data": {
                    "target_vtherm_unique_id": "Target VTherm entity",
                    "exclusion_patterns": "Excluded fan modes (regex)"
                },
                "data_description": {
                    "exclusion_patterns": "Fan modes matching one of these patterns get no threshold. A fixed name matches exactly (e.g. off); use wildcards for a partial match (e.g. .*auto.*)."
                }
            },
            "reconfigure": {
                "title": "Auto Fan — excluded fan modes",
                "data": {
                    "exclusion_patterns": "Excluded fan modes (regex)"
                },
                "data_description": {
                    "exclusion_patterns": "Fan modes matching one of these patterns get no threshold. A fixed name matches exactly (e.g. off); use wildcards for a partial match (e.g. .*auto.*)."
                }
            }
        },
        "error": {
            "invalid_entity": "The selected entity is not a valid Versatile Thermostat.",
            "invalid_regex": "One of the exclusion patterns is not a valid regular expression."
        },
        "abort": {
            "already_configured": "This thermostat is already configured.",
            "reconfigure_successful": "Configuration updated and integration reloaded."
        }
    }
}